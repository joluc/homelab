apiVersion: v2
name: redis
version: 1.0.0
description: Redis instance for homelab (Paperless, etc.)

dependencies:
  - name: redis
    version: 25.5.3
    repository: https://charts.bitnami.com/bitnami
