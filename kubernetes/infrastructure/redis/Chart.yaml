apiVersion: v2
name: redis
version: 1.0.0
description: Redis instance for homelab (Paperless, etc.)

dependencies:
  - name: redis
    version: 28.2.4
    repository: https://charts.bitnami.com/bitnami
