apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-config
  labels:
    app.kubernetes.io/name: {{ .Chart.Name }}
    app.kubernetes.io/instance: {{ .Release.Name }}
data:
  {{- if .Values.config.customEndpoint }}
  HERMES_CUSTOM_ENDPOINT: {{ .Values.config.customEndpoint | quote }}
  {{- end }}
