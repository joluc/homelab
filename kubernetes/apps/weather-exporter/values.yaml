weather-exporter:
  nameOverride: "weather-exporter"
  fullnameOverride: "weather-exporter"

  image:
    repository: ghcr.io/joluc/weather-exporter
    tag: "0.2.0"

  serviceMonitor:
    enabled: true
    interval: 30s
    labels:
      prometheus: kube-prometheus

  config:
    cities:
      - "Leipzig:51.33,12.37@dwd"
      - "Copenhagen:55.67,12.56@yr"

    providers:
      dwd:
        enabled: true
      yr:
        userAgent: ""
      openweathermap:
        enabled: false
        apiKey: ""

    cacheTTL: "5m"
    jsonLogs: false

  resources:
    limits:
      cpu: 200m
      memory: 128Mi
    requests:
      cpu: 100m
      memory: 64Mi
