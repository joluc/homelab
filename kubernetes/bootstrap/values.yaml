# Bootstrap Configuration

# Repository settings
repoURL: https://github.com/joluc/homelab.git
targetRevision: main

# Directories to scan - each app deploys to its own namespace (using app name)
applications:
  - name: infrastructure
    path: kubernetes/infrastructure
  - name: observability
    path: kubernetes/observability
  - name: apps
    path: kubernetes/apps
