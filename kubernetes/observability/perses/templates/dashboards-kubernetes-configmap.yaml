apiVersion: v1
kind: ConfigMap
metadata:
  name: perses-dashboards-kubernetes
  namespace: perses
  labels:
    perses.dev/dashboard: "true"
data:
{{- range $path, $_ := .Files.Glob "dashboards/kubernetes/*.yaml" }}
  {{ base $path }}: |-
{{ $.Files.Get $path | indent 4 }}
{{- end }}
