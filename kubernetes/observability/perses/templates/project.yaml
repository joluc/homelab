apiVersion: v1
kind: ConfigMap
metadata:
  name: perses-project
  namespace: perses
  labels:
    perses.dev/resource: "true"
data:
  project.yaml: |
    kind: Project
    metadata:
      name: perses
    spec:
      display:
        name: Perses
        description: Main observability project with Kubernetes, Prometheus, and Node Exporter dashboards
