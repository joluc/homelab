apiVersion: v1
kind: ConfigMap
metadata:
  name: perses-dashboards-node-exporter
  namespace: perses
  labels:
    perses.dev/resource: "true"
data:
{{- range $path, $_ := .Files.Glob "dashboards/node-exporter/*.yaml" }}
  {{ base $path }}: |-
{{ $.Files.Get $path | indent 4 }}
{{- end }}
