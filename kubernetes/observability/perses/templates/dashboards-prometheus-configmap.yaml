apiVersion: v1
kind: ConfigMap
metadata:
  name: perses-dashboards-prometheus
  namespace: perses
  labels:
    perses.dev/resource: "true"
data:
{{- range $path, $_ := .Files.Glob "dashboards/prometheus/*.yaml" }}
  {{ base $path }}: |-
{{ $.Files.Get $path | indent 4 }}
{{- end }}
