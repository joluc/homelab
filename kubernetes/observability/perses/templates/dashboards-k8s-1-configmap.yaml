apiVersion: v1
kind: ConfigMap
metadata:
  name: perses-dashboards-k8s-1
  namespace: perses
  labels:
    perses.dev/dashboard: "true"
data:
{{- range $path, $_ := .Files.Glob "dashboards/kubernetes-1/*.yaml" }}
  {{ base $path }}: |-
{{ $.Files.Get $path | indent 4 }}
{{- end }}
