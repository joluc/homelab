apiVersion: v1
kind: ConfigMap
metadata:
  name: perses-dashboards-k8s-3
  namespace: perses
  labels:
    perses.dev/dashboard: "true"
data:
{{- range $path, $_ := .Files.Glob "dashboards/kubernetes-3/*.yaml" }}
  {{ base $path }}: |-
{{ $.Files.Get $path | indent 4 }}
{{- end }}
