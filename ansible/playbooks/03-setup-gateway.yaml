---
- name: Setup Gateway Node (Caddy)
  hosts: gateways
  become: true
  vars:
    # Email is defined in group_vars/secret.yaml
    target_upstreams: "{{ groups['control_plane'] | map('extract', hostvars, ['ansible_host']) | list }}"
    # Add subdomains here as you deploy services.
    # Entries can be either a bare string (routes to the control-plane on :80)
    # or a mapping { name, upstream } where upstream is host:port.
    subdomains:
      - analytics
      - app
      - argocd
      - auth
      - backstage
      - dash
      - dns
      - docs
      - grafana
      - ha
      - home
      - jaeger
      - { name: kino,          upstream: "192.168.1.102:8096" }
      - mc
      - { name: metrics-proxy, upstream: "100.97.29.99:80" }
      - movies
      - n8n
      - { name: nas,           upstream: "192.168.1.102:5000" }
      - notes
      - opensearch
      - perses
      - photos
      - portal
      - preview
      - prometheus
      - pyroscope
      - sentry
      - status
      - unifi
      - vault
      - wol

  tasks:
    - name: Install dependencies
      apt:
        name:
          - debian-keyring
          - debian-archive-keyring
          - apt-transport-https
          - curl
        state: present
        update_cache: yes

    - name: Add Caddy GPG key
      apt_key:
        url: https://dl.cloudsmith.io/public/caddy/stable/gpg.key
        state: present
        keyring: /usr/share/keyrings/caddy-stable-archive-keyring.gpg

    - name: Add Caddy repository
      apt_repository:
        repo: "deb [signed-by=/usr/share/keyrings/caddy-stable-archive-keyring.gpg] https://dl.cloudsmith.io/public/caddy/stable/deb/debian any-version main"
        state: present
        filename: caddy-stable

    - name: Install Caddy
      apt:
        name: caddy
        state: present
        update_cache: yes

    - name: Enable IP Forwarding (for VPN/Routing)
      sysctl:
        name: net.ipv4.ip_forward
        value: '1'
        sysctl_set: yes
        state: present
        reload: yes

    - name: Configure Caddyfile
      copy:
        dest: /etc/caddy/Caddyfile
        content: |
          {
              email {{ email }}
          }

          {% for sub in subdomains %}
          {% set name = sub.name if sub is mapping else sub %}
          {% set upstream = sub.upstream if sub is mapping else target_upstreams[0] ~ ':80' %}
          {{ name }}.joluc.de {
              reverse_proxy http://{{ upstream }} {
                  header_up Host {host}
                  header_up X-Real-IP {remote_host}
                  header_up X-Forwarded-For {remote_host}
                  header_up X-Forwarded-Proto {scheme}
              }
          }
          {% endfor %}
      notify: Reload Caddy

  handlers:
    - name: Reload Caddy
      service:
        name: caddy
        state: reloaded
        enabled: yes
